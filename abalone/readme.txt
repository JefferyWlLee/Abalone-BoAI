how to start the game:
    step 1: open terminal and navigate to where requirements.txt is located
    step 2: run the command "pip install -r requirements.txt"
    step 3: run the command "python run_game.py"